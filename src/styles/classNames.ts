import { BG_COLOR } from "./colors";

export const center = "flex justify-center items-center";

export const customDot = `before:content-[''] before:absolute before:top-1/2 before:left-0 before:w-[4px] before:h-[4px] before:${BG_COLOR.black} before:rounded-full`;
